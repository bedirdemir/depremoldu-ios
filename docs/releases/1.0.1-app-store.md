# 1.0.1 — App Store Güncelleme Upload Kaydı

- Tarih: 2026-09-21
- Kaynak: `main` · `v1.0.1` tag'i (PR zinciri: `fix/system-loading-indicator` → `dev` → `test` → `main`).
- Sürüm/build: `1.0.1` (3)
- Bundle ID: `org.depremoldu.app`
- Şema/konfigürasyon: `DepremOldu-Release` (Release)
- İmzalama: Apple Distribution (otomatik), Team `648V76LNZW`
- Dağıtım: `xcodebuild -exportArchive` (`method=app-store-connect`, `destination=upload`)
- Sonuç: **Upload succeeded** — paket processing durumunda
- Kapsam: App Store güncellemesi; TestFlight turu ürün sahibi kararıyla atlandı.
- Değişiklik: Açılış/arka plan yeniden doğrulama yükleme göstergesi marka kırmızısı yerine küçük, sistem varsayılan renkli `ProgressView` olarak gösteriliyor; gösterge yumuşak yay animasyonuyla açılıp kapanıyor ve yükleme bitince liste içeriği yukarı kayarak yerleşiyor. Kullanıcı pull-to-refresh göstergesi sistemin kendi kontrolü olarak değişmeden kalıyor.
- App Store Connect "What's New" önerisi: "Açılış ve yenileme yükleme göstergesi iyileştirildi: gösterge artık daha sade ve yükleme tamamlanınca yumuşak bir animasyonla kayboluyor."
- Ürün sahibi adımları: App Store Connect'te 1.0.1 sürümünü oluştur, build 3'ü ekle, "What's New" gir ve incelemeye gönder (TestFlight gerekmiyor).

## Doğrulama Kanıtı

- `make version-check`: ok (1.0.1 build 3, org.depremoldu.app).
- `make verify`: version-check, project-check, paket testleri 57/57, Development + Release build, app unit testleri 17/17 geçti. UI suite ilk koşuda harita testinin zamanlama flake'i yüzünden kırmızı oldu; seçili tekrar ve takip eden tam koşuda UI 11/11 geçti (1 mağaza ekran görüntüsü testi skip).
- Archive: `.build/DepremOldu.xcarchive` (`CFBundleShortVersionString = 1.0.1`, `CFBundleVersion = 3`, `ITSAppUsesNonExemptEncryption = false`).
- Upload log: `Uploaded package is processing` / `Upload succeeded`.
